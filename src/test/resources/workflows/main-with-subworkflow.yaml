name: main-with-subworkflow
steps:
  - states: ["0", "1"]
    actions:
      - [coordinator, executeSubWorkflow, "simple-math.yaml"]
  - states: ["1", "end"]
    actions:
      - [coordinator, executeSubWorkflow, "simple-math.yaml"]
