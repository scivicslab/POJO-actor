name: conditional-branch-workflow
steps:
  - states: ["0", "1"]
    actions:
      - [decision, setValue, "15"]
  - states: ["1", "2"]
    actions:
      - [decision, checkValue, ""]
  - states: ["2", "end"]
    actions:
      - [decision, processHigh, ""]
