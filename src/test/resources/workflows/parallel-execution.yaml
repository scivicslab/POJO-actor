name: parallel-execution-workflow
steps:
  - states: ["0", "1"]
    actions:
      - [actor1, execute, "task1"]
      - [actor2, execute, "task2"]
      - [actor3, execute, "task3"]
  - states: ["1", "end"]
    actions:
      - [actor1, execute, "finalize"]
